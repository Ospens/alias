import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  buttonBack: {
    flex: 1,
  },
  row: {
    marginBottom: 15,
    minHeight: 32,
  },
});
